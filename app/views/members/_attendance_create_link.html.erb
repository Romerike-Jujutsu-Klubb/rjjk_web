<%=link_to ('&nbsp;' * 4).html_safe, {:controller => :attendances, :action => :create, :attendance => {:member_id => member_id, :group_schedule_id => group_schedule_id, :year => date.year, :week => date.cweek}}, :method => :post, :remote => true, :style => 'text-decoration: none' %>

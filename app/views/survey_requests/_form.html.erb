<%= form_for(@survey_request) do |f| %>
  <% if @survey_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_request.errors.count, "error") %> prohibited this survey_request from being saved:</h2>

      <ul>
      <% @survey_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :survey_id %><br>
    <%= f.number_field :survey_id %>
  </div>
  <div class="field">
    <%= f.label :member_id %><br>
    <%= f.number_field :member_id %>
  </div>
  <div class="field">
    <%= f.label :completed_at %><br>
    <%= f.datetime_select :completed_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

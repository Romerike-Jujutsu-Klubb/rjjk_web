Har du problemer med <%= textify 'å' %> lese denne e-posten, klikk her:
<%= url_for :only_path => false, :controller => :events, :action => :show, :id => @event_invitee.event.id %>

<%= textify @event_invitee.event.name %>

<%= textify @event_invitee.event.body if @event_invitee.event.body %>

<%= form_for(@board_meeting) do |f| %>
  <% if @board_meeting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@board_meeting.errors.count, 'error') %> prohibited this board_meeting from being saved:</h2>

      <ul>
      <% @board_meeting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :start_at, 'Tidspunkt' %><br />
    <%= f.text_field :start_at, class: 'datetime form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :minutes, 'Referat' %> <%=@board_meeting.minutes_filename%>
    <%= f.file_field :minutes, class: 'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit 'Lagre', class: 'btn btn-primary' %>
    <%= link_to 'Vis', @board_meeting %> |
    <%= link_to 'Tilbake', board_meetings_path %>
  </div>
<% end %>

- if members.present?
  .col.mb-4: .card.h-100
    h3.card-header
      .badge.badge-info.float-right #{members.size}
      = title
    .card-body
      table.table.table-sm
        tbody
          - members.each do |m|
            tr
              td
                => link_to m.name, m
                - if m.age
                  ' (#{m.age} år)
              td.text-right
                - user = m.user
                - phone = user.phones.first
                - if phone
                  a href="sms:/#{phone}/" title="Send SMS"
                    i.fa.fa-sms
                  a.ml-2 href="tel:#{phone}" title="Ring"
                    i.fa.fa-phone
                - else
                  a href="mailto:#{user.email}" title="Send e-post"
                    i.fa.fa-envelope

<h1>Hei <%= @recipient.first_name %>!</h1>

<% unless @practice.message.blank? %>
    <p><%= @practice.message %></p>
<% end %>

Følgende er påmeldt til trening i <%= @group_schedule.start_at.day_phase %>:

<ul>
  <% @members.sort_by(&:name).each do |m| %>
      <li><%= m.name %></li>
  <% end %>
</ul>

Du kan se på og endre din påmelding på <%= link_to 'Mitt oppmøte', @email_url %>.

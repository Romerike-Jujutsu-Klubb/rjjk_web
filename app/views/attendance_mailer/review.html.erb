<h1>Hei <%= @member.first_name %>!</h1>

<h2><%= @title %></h2>

<p>
  Vi starter nå en prøveordning for voksengruppen der dere selv bekrefter at
  dere var på trening, eller registrerer at dere var borte. Dette vil spare oss
  for arbeid med å registrere oppmøtet på trening, og å føre oppmøtet fra
  listene inn i medlemsregisteret.
</p>

<h2>Treningen i dag</h2>
<ul>
  <% @completed_attendances.each do |a| %>
      <li>
        <%= a.group_schedule.start_at.strftime('%H:%M') %>-<%= a.group_schedule.end_at.strftime('%H:%M') %>
        <%= a.group_schedule.group.name %>
        [<%= link_to 'Var der!', UserSystem.with_login(@member.user, :controller => :attendances, :action => :review, :id => a.id, :attendance => {:status => Attendance::Status::ATTENDED}) %>]
        &nbsp;&nbsp;[<%= link_to 'Var syk!', UserSystem.with_login(@member.user, :controller => :attendances, :action => :review, :id => a.id, :attendance => {:status => Attendance::Status::SICK}) %>]
        &nbsp;&nbsp;[<%= link_to 'Bortreist!', UserSystem.with_login(@member.user, :controller => :attendances, :action => :review, :id => a.id, :attendance => {:status => Attendance::Status::HOLIDAY}) %>]
        &nbsp;&nbsp;[<%= link_to 'Forhindret!', UserSystem.with_login(@member.user, :controller => :attendances, :action => :review, :id => a.id, :attendance => {:status => Attendance::Status::ABSENT}) %>]
      </li>
  <% end %>
</ul>

<% if @older_attendances.any? %>
    <h3>Tidligere treninger du ikke har bekreftet enda:</h3>
    <ul>
      <% @older_attendances.each do |a| %>
          <li>
            <%= a.date %>
            <%= t(:date)[:day_names][a.date.cwday] %>
            <%= a.group_schedule.group.name %>
            [<%= link_to 'Var der!', UserSystem.with_login(@member.user, :controller => :attendances, :action => :review, :id => a.id, :attendance => {:status => Attendance::Status::ATTENDED}) %>]
            &nbsp;&nbsp;[<%= link_to 'Var syk!', UserSystem.with_login(@member.user, :controller => :attendances, :action => :review, :id => a.id, :attendance => {:status => Attendance::Status::SICK}) %>]
            &nbsp;&nbsp;[<%= link_to 'Bortreist!', UserSystem.with_login(@member.user, :controller => :attendances, :action => :review, :id => a.id, :attendance => {:status => Attendance::Status::HOLIDAY}) %>]
            &nbsp;&nbsp;[<%= link_to 'Forhindret!', UserSystem.with_login(@member.user, :controller => :attendances, :action => :review, :id => a.id, :attendance => {:status => Attendance::Status::ABSENT}) %>]
          </li>
      <% end %>
<% end %>
</ul>

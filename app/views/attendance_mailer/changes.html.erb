<% if @new_attendees.any? %>
    Nylig påmeldt:

    <ul>
      <% @new_attendees.each do |m| %>
          <li><%= m.name %></li>
      <% end %>
    </ul>
<% end %>

<% if @new_absentees.any? %>
    Avmelding:
    <ul>
      <% @new_absentees.each do |m| %>
          <li><%= m.name %></li>
      <% end %>
    </ul>
<% end %>

Du kan se på og endre din påmelding på <%= link_to 'Mitt oppmøte', @email_url %>.

<% if @attendees.any? && @attendees != @new_attendees %>
    <%= @attendees.size %> deltakere påmeldt:
    <ul>
      <% @attendees.each do |m| %>
          <li><%= m.name %></li>
      <% end %>
    </ul>
<% end %>

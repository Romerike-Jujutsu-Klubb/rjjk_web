<%= link_to 'Ny rolle', new_role_path, class: 'btn btn-primary float-right' %>
<h1>Definerte roller i RJJK</h1>

<table class="table">
  <tr>
    <th>Navn</th>
    <th>År i styret</th>
    <th></th>
  </tr>

  <% @roles.each do |role| %>
      <tr>
        <td><%= link_to role.name, edit_role_path(role) %></td>
        <td align="center"><%= role.years_on_the_board %></td>
        <td>
          <% if role.elections.any? %>
              <!-- <%= role.elections.size %> valg -->
          <% else %>
              <%= link_to('', role, method: :delete, data: {confirm: 'Er du sikker?'},
                      class: 'fa fa-trash') %>
          <% end %>
        </td>
      </tr>
  <% end %>
</table>

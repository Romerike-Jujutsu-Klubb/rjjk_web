h2.float-right
  - if @user.deleted?
    span.text-danger.mr-3 Brukeren er slettet
    = link_to restore_user_path(@user), method: :patch, class: 'btn btn-warning'
      i.fa.fa-undo>
      | Gjenopprett
  - else
    a.btn.btn-warning.mr-2 href=with_detour(user_merge_path(@user))
      i.fa.fa-user-friends>
      | Slå sammen med annen bruker
    a.btn.btn-secondary.mr-2 href=user_path(@user, format: :vcf)
      i.fa.fa-address-card>
      | Kontaktkort
    = link_to user_path(@user), method: :delete, class: 'btn btn-danger', data: {confirm: 'Er du sikker på at du vil slette denne brukeren?'}
      i.fa.fa-trash>
      | Slett bruker

h2.clearfix Rediger bruker

- if (error_msgs = @user.errors.full_messages_for(:base)).any?
  .alert.alert-danger
    ' Brukeren
    = error_msgs.join('  ')

= render 'form', user: @user, submit: !@user.deleted?

/ - unless @user.deleted?
/   br
/   = form_tag(url_for(action: :change_password)) do
/     = render 'password', submit: true

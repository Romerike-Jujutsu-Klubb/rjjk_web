<h1>Listing applications</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Rank</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @technique_applications.each do |application| %>
  <tr>
    <td><%= application.name %></td>
    <td><%= application.rank_id %></td>
    <td><%= link_to 'Show', application %></td>
    <td><%= link_to 'Edit', edit_technique_application_path(application) %></td>
    <td><%= link_to 'Destroy', application, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Application', new_technique_application_path %>

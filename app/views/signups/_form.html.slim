= bootstrap_form_for @signup do |f|
  = f.collection_select :user_id, @users, :id, :name, {prompt: true}, class: :chosen
  = f.collection_select :nkf_member_trial_id, @nkf_member_trials, :id, :name, {prompt: true}, class: :chosen
  .actions = f.submit

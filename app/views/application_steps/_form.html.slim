= bootstrap_form_for(@application_step) do |f|
  - if @application_step.errors.any?
    div id="error_explanation"
      h2
        => pluralize(@application_step.errors.count, "error")
        | prohibited this application_step from being saved:

      ul
        - @application_step.errors.full_messages.each do |msg|
          li = msg

  = f.collection_select :technique_application_id, @technique_applications, :id, :name
  / = f.number_field :position
  = f.text_area :description

  = render 'images/inline_form', f: f

  .text-right
    = f.primary :Lagre

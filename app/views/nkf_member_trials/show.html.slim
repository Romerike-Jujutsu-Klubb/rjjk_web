.float-right
  a.btn.btn-link href=nkf_member_trials_path
    i.fa.fa-table>
    | NKF Prøvemedlemmer
  - if @nkf_member_trial.signup.nil?
    a.btn.btn-primary.ml-3 href=new_signup_path(signup: {nkf_member_trial_id: @nkf_member_trial.id})
      i.fa.fa-sign-in>
      | Registrer #{@nkf_member_trial.name}

h1 NKF Prøvemedlem (#{@nkf_member_trial.id})

table.table.table-sm
  tbody
    - @nkf_member_trial.attributes.except('id', 'created_at', 'updated_at').sort.each do |k,v|
      tr
        th = k
        td = v

<div title="<%= title_helper %>" class="form">
  <%= head_helper 'Glemt passord' %>

  <div class="form-padding">
    <p>
      Skriv inn e-postadressen din i feltet under, og klikk
      <strong>Send veiledning</strong>
      for å motta en e-post med beskrivelse av hvordan du skal få tak i
      passordet ditt.
    </p>

    <%= form_tag(url_for(action: "#{controller.action_name}")) do %>
        <p>
          <%= text_field :user, :email, size: 30, placeholder: 'e-post',
                  class: 'form-control' %>
        </p>

        <div class="button-bar">
          <%= submit_tag 'Send veiledning', class: 'btn btn-primary' %>
          <%= link_to 'Avbryt', {action: 'login'}, class: 'btn btn-default' %>
        </div>
    <% end %>
  </div>
</div>

<%= javascript_tag do %>
    $('#user_email').focus();
<% end %>

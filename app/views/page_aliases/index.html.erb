<h1>Listing page_aliases</h1>

<table class="table">
  <tr>
    <th>Gammel URL</th>
    <th>Ny URL</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @page_aliases.each do |page_alias| %>
  <tr>
    <td><%= page_alias.old_path %></td>
    <td><%= page_alias.new_path %></td>
    <td><%= link_to 'Endre', edit_page_alias_path(page_alias) %></td>
    <td><%= link_to 'Slett', page_alias, method: :delete, data: { confirm: 'Er du sikker på at du vil slette sidealiaset?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nytt sidealias', new_page_alias_path %>

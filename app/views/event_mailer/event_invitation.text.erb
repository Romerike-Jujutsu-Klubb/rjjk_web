Har du problemer med <%#= textify 'å' %> lese denne e-posten, klikk her:
<%= url_for :only_path => false, :controller => :events, :action => :show, :id => @event.id %>

<%= textify @event.name %>

<%= textify @event.ingress %>

<%= textify @event.body if @event.body %>

Ønsker du ikke flere nyhetsbrev fra oss, vennligst klikk på denne linken:
<%= url_for :only_path => false, :controller => :events, :action => :unsubscribe, :email => @email %>

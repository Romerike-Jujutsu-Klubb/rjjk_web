Har du problemer med å lese denne e-posten, klikk her:
<%= url_for :only_path => false, :controller => :events, :action => :show, :id => @event_invitee.event.id %>

<%= textify @event_invitee_message.subject %>

<%= textify @event_invitee_message.body %>

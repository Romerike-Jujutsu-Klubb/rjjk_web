<h1>Signaturer</h1>

<table class="table">
  <tr>
    <th>Medlem</th>
    <th>Bilde</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @signatures.each do |signature| %>
  <tr>
    <td><%= signature.member.name %></td>
    <td>  <image src="<%=url_for(:action => :image, :id => signature.id)%>"/></td>
    <td><%= link_to 'Vis', signature %></td>
    <td><%= link_to 'Endre', edit_signature_path(signature) %></td>
    <td><%= link_to 'Slett', signature, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Registrer signatur', new_signature_path %>

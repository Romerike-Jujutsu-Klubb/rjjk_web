h1 Endre spørreundersøkelse

= render 'form'

h3 Spørsmål

table.table
  - @survey.survey_questions.sort_by(&:position).each do |question|
    tr
      td
        = question.title
        .help-block = question.choices
      td =link_to 'Endre', edit_survey_question_path(question), class: 'btn btn-default'

.btn-toolbar
  = back_or_link_to 'Tilbake', surveys_path, class: 'btn btn-default'
  = link_to 'Vis', @survey, class: 'btn btn-default'
  = link_to 'Legg til spørsmål', \
        new_survey_question_path(survey_question: {survey_id: @survey.id}), \
        class: 'btn btn-primary'
